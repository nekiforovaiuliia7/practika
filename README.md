# practika
Планирование

Составлен план: настройка → проектирование форм → система ответов → аналитика → деплой.

Проблема: не учтены разные типы вопросов (выбор, шкала, текст) в ТЗ.

Проектирование БД

Созданы таблицы:

Опросы (id, название, дата_старта, дата_окончания)

Вопросы (id, текст, тип, id_опроса)

Ответы (id, данные, id_пользователя, id_вопроса).

Ошибка: забыл связь ответов с опросами (добавил позже).

Базовый функционал

Реализовано: создание опросов, прохождение анкет, просмотр результатов.

Проблемы:

Нет проверки сроков опроса (можно отвечать после окончания).

Путаница с типами вопросов (хранил все ответы в текстовом поле).

Интерфейс

Адаптивные формы для разных типов вопросов (чекбоксы, радиокнопки, текстовые поля).

Ошибка: динамическое добавление вопросов ломало верстку.

Дополнительные функции

Добавлена статистика: % ответов по вариантам, текстовые облака.

Ошибка: при удалении опроса не удалялись связанные вопросы (мусор в БД).

Тестирование

Найдены уязвимости:

Подделка id опроса (можно отвечать на неактивные).

XSS в текстовых ответах.

Решение: валидация активных опросов и экранирование.

Деплой

Развернуто на PythonAnywhere.

Проблемы: зависание при 50+ одновременных ответах (нет кэширования).

Рекомендации по исправлению ошибок:
Безопасность данных

Добавить роли пользователей:

python
if not current_user.is_admin:  
    abort(403)  # Только админы создают опросы  
Шифровать персональные данные в ответах (если есть).

Оптимизация БД

Разделить таблицу ответов по типам:

Ответы_выбор (id_варианта)

Ответы_текст (текст)

Внедрить каскадное удаление: при удалении опроса → автоматическое удаление вопросов/ответов.

Валидация операций

Проверять сроки опроса перед сохранением ответа:

python
if datetime.now() > survey.end_date:  
    flash("Опрос завершен")  
    return redirect(url_for('index'))  
Добавить лимиты на текстовые ответы (200 символов).

Производительность

Кэшировать статистику с помощью Redis:

python
@cache.cached(timeout=300)  
def get_stats(survey_id):  
    # расчет статистики  
Использовать пагинацию для списка ответов.

Юзабилити форм

Реализовать динамическое добавление вопросов через JavaScript (без перезагрузки).

Добавить прогресс-бар при прохождении длинных опросов.

Тестирование

Написать скрипты нагрузки (50+ пользователей).

Проверить обработку некорректных данных: пустые ответы, несуществующие id.
